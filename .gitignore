# Generated Neovim wrappers and removal manifest are local build artifacts.
home/.chezmoiremove

home/AppData/Local/nvim
home/dot_config/nvim

worktrees

# Environment files (never commit secrets)
.env
.env.*
*.env

# Allow templates (safe to commit)
!.env.example
!.env.sample
!.env.template
!.env.dist
!.env.defaults
