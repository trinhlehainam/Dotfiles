session: dotfiles
root: ~/.local/share/chezmoi
attach: true

windows:
  - name: workmux
    selected: true
    commands:
      - workmux dashboard --session --tab worktrees
  - name: main
    commands:
      - nvim
